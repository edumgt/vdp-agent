[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "corp-shared"
version = "0.1.0"
description = "공통 유틸(ID 생성, env 헬퍼, 시간)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["shared*"]
