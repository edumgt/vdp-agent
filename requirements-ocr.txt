# 실제 사전학습 DL OCR 모델(EasyOCR: PyTorch 기반 CRNN)을 사용하려면 설치하세요.
# torch/torchvision 포함 용량이 크고(수백 MB~1GB+), 최초 실행 시 언어 가중치를 다운로드합니다(네트워크 필요).
# 설치 후 .env의 OCR_PROVIDER=easyocr 로 전환하면 됩니다. 기본값(fixture)은 이 설치 없이도 동작합니다.
easyocr>=1.7
